- name: Cmd1
  ansible.builtin.command: echo {{ used_var }}
  changed_when: false

- name: Cmd2
  ansible.builtin.command:
    cmd: echo {{ used_var }}
  changed_when: false

- name: Verify ip address
  ansible.builtin.assert:
    that: "{{ '1.1.1.1' is ansible.utils.ipv4_address }}"
